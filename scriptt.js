function kajal(e){
    let val=e.value;
    var textarea=document.getElementById('txt');
    textarea.style.fontSize=val+"px";
}
function bgclr(e){
    let val=e.value;
    var textarea=document.getElementById('bclor');
    textarea.style.fontSize=val+"px";
}